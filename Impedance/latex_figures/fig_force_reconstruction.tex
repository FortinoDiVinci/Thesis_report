\begin{figure} [h]
\centering
	\shorthandoff{;} 
	\renewcommand{\thesubfigure}{\arabic{subfigure}}
	\foreach \i in {1,...,11} {%
    	\subfloat[]{%
        \centering
        \begin{tikzpicture}
        \definecolor{matlabBlue}{RGB}{0,114,189}
        \definecolor{vermillon}{RGB}{213,94,0}

        \begin{axis}[%
			width=.25\columnwidth,
			scale only axis,   
			axis x line = middle,
			axis y line = middle, 
			xlabel={$ t (ms) $},
%			\ifthenelse{%
%				\equal{\i}{1}%
%				\OR%
%				\equal{\i}{4}%
%				\OR%
%				\equal{\i}{7}%
%				\OR%
%				\equal{\i}{10}%
%				}%
%				{}%
%				{}%	
			xlabel style={font=\footnotesize},	
			xlabel near ticks,	
			%xlabel style={font=\footnotesize, at={(axis description cs:0.5,-0.01)}, anchor=north},
			ylabel={$ \delta f_z (N) $},
			ylabel style={font=\footnotesize},
			%ylabel style={font=\footnotesize, at={(axis description cs:-0.1,.5)},rotate=90, anchor=north},
			ylabel near ticks,			
			tick label style={font=\footnotesize, fill=white},
			grid=both,
            grid style={line width=.1pt, draw=gray!10},
            major grid style={line width=.2pt,draw=gray!50},
            minor tick num=4,
            table/col sep=semicolon,
			]
			% perturbed trajectory
			% delta force
			\addplot [
			solid,
			matlabBlue,
			line width=1.25pt,
			] table [x index= 0, y index=\number\numexpr\i*4-3\relax] 
			{misc/forces_reconstruction.csv};
			% reconstructed delta force
			\addplot [
			densely dashed,
			vermillon,
			line width=1.25pt,
			] table [x index=0, y index=\number\numexpr\i*4-2\relax] {misc/forces_reconstruction.csv};
			% non perturbed trajectory
			% delta force
			\addplot [
			solid,
			matlabBlue,
			line width=.5pt,
			] table [x index= 0, y index=\number\numexpr\i*4-1\relax] 
			{misc/forces_reconstruction.csv};
			% reconstructed delta force
			\addplot [
			densely dashed,
			vermillon,
			line width=.5pt,
			] table [x index=0, y index=\number\numexpr\i*4\relax] {misc/forces_reconstruction.csv};
			
        \end{axis}
        \end{tikzpicture}
    	}
    	\ifthenelse{%
    		\equal{\i}{3}%
    		\OR%
    		\equal{\i}{6}%
    		\OR%
    		\equal{\i}{9}%
    		}%
    	{\\}%
    	{}%
	}%	
	\shorthandon{;} 
    \caption{Reconstructed forces for the 11 perturbations selected. Solid lines represent $ \delta f_z $ and dashed lines reresent the reconstructed force using the identified parameters of KBI model. Thiner lines are for the evaluation when no perturbation occured.} 
    \label{fig_force_reconstruction}
\end{figure}